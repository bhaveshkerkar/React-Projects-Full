# React + Vite

This project uses **React** with **Vite**, providing a fast and minimal development setup with Hot Module Replacement (HMR) and basic ESLint rules.

## React Plugins

Vite supports two official plugins for React:

- **@vitejs/plugin-react** – uses **Babel** for Fast Refresh
- **@vitejs/plugin-react-swc** – uses **SWC** for Fast Refresh and faster builds

## ESLint Configuration

For production-ready applications, it is recommended to use **TypeScript** with **type-aware linting** enabled.  
You can explore the official React + TypeScript template to learn how to integrate TypeScript with `typescript-eslint`:

👉 https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts
